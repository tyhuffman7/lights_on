# Lights On — handoff (2026-09-21)

PAPER ONLY / ORDER DISABLED. This checkpoint implements a reusable category-neutral price screen. The September 20 21:10–21:30 UTC audit is old evidence, not a completed subsequent comparison. At task start, the original workspace was on `main` with pre-existing staged/unstaged work; that work was preserved. The prior isolated checkout and published PR #3 both resolve to `049cbb1`; all existing lib/worker sources matched the actual working source. No later coverage capture was found in the inspected local evidence inventory.

- New implementation: [screen documentation](EXECUTABLE-SCREEN.md). Catalog/matching/streams/book normalization/depth pricing are reused. Candidate routes survive shared subscription deduplication; category caps and same-day/horizon/strategy labels are explicit. Taker/taker prices and hypothetical resting-bid economics are separate.
- Verification: attached independent mechanics checks pass against actual working source on Node 22.23.2. Twenty-two focused screen, reference, supervisor, stream and pricing tests pass. Queue discrepancy and fee-accounting scenarios remain explicit; passive-fill production source and historical results are unchanged.
- Capture: preparation in progress for one frozen 30-minute supervised screen, maximum 60 candidate routes, $100 simulated capital ($50/venue), fixed baseline economics and separate recovery reservations. No orders, ledger access, strategy search, recovery refinement, or automatic restart.
- Evidence directory: original workspace `work/executable-screen-20260921`. New code checkout: `/private/tmp/lights-on-executable-screen-20260921`, branch `codex/executable-price-screen`, based on `049cbb1`. Publish only new scoped source/tests/docs and sanitized summaries. Never publish credentials, raw evidence, databases or private account information.
- Publication and hosted full CI: pending. Next action is finish preparation, launch once after verification, report actual coverage/results, create a signed non-main draft PR, read CI compactly once, update this handoff and stop.

Commands for this checkpoint: pinned Node `worker/executable-screen.ts prepare OUTPUT`; then `scripts/executable-screen-launch.mjs OUTPUT ENV_FILE`. Do not repeat a started output directory or launch another capture. The single-use launcher and worker enforce this. Refer to the screen documentation for exact invocation and evidence definitions.

Historical sizing/recovery remains no-fill evidence; fresh-fill recovery is unproven. Historical ledgers and halt remain unchanged. Existing evidence: [missed-opportunity report](../pilot/MISSED-OPPORTUNITY-2026-09-20.md), [sizing/recovery report](../pilot/SIZING-RECOVERY-2026-09-20.md), [draft PR #3](https://github.com/tyhuffman7/lights_on/pull/3).
