# Lights On — September 22, 2026 (UTC)

PAPER ONLY / LIVE ORDERS DISABLED. The user authorized one multi-market integration-and-session milestone, superseding the old KSU-only stop. No live launch, broad scan, matcher rewrite, extra strategy or open-ended work.

**Current:** the existing prepared checkout `/private/tmp/lights-on-depth-discovery-20260922` is now on `codex/multi-market-paper`, based on reviewed shortlist commit `92f5ffc`. No duplicate paper process was running at task start. The original main checkout and its unrelated changes/index are preserved. The earlier unpublished KSU components are reused; the September 22 05:12–05:35 UTC [KSU report](KSU-CONDITIONAL-PAPER.md) and [results](ksu-paper-results-20260922.json) are preserved without rerunning collection.

**Implemented:** streamed retained-route changes → exact-quantity requested-book confirmation → automatic independent PAPER legs, limited to the five deeply reviewed candidates and exact purchase directions. See [scope/settings/risks](MULTI-MARKET-PAPER.md). $100 total simulated cash, $50/venue, quantities through ten, $15 entry reservation, $45 commitment, three open pairs, five attempts, one active sequence, strictly positive after unchanged fees and 2¢ risk per pair. Existing 500 ms transport and bounded recovery retained. Known exposure never clears; locked cash cannot recycle. At most three transport reconnections within the one original 60-minute deadline.

**Verified before collection:** 42 focused tests passed; worker import and diff whitespace checks passed. Synthetic favorable books prove all five orientations reach both modeled legs; they are not observed market fills or profit. Full tests/typecheck/build await hosted CI. Preparation/session have not launched yet. Signed publication is being attempted using the unchanged 1Password signer; its prior failure remains an unresolved risk until a signed commit succeeds.

**Next action:** finish scoped publication/CI, refresh the 34 retained comparisons and exact primary versions, freeze, launch one supervised session, publish sanitized actual outcomes, update this short handoff and stop. No tests/builds during collection, repeated AI market polling, session extension or new automation.

Commands in prepared checkout: `node --experimental-strip-types --test tests/multi-paper.test.ts tests/ksu-paper.test.ts tests/book-confirmation-adapter.test.ts tests/integrated-confirmation.test.ts`; `node --experimental-strip-types worker/multi-paper.ts prepare OUTPUT_DIRECTORY`; `node scripts/multi-paper-launch.mjs OUTPUT_DIRECTORY ENV_FILE`. Preparation/launch directories are single-use. Never use the prior KSU evidence directory. Full verification: GitHub CI on the authorized non-main draft PR to `tyhuffman7/lights_on`.
